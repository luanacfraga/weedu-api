# =================================
# TOOLDO API - Environment Variables
# =================================

# =================================
# Application
# =================================
NODE_ENV=development
PORT=3000

# =================================
# Database (PostgreSQL)
# =================================
# Local development (Docker)
DATABASE_URL="postgresql://tooldo:tooldo123@localhost:5433/tooldo_db?schema=public"

# Production (AWS RDS example)
# DATABASE_URL="postgresql://user:password@your-rds-endpoint.us-east-1.rds.amazonaws.com:5432/tooldo_prod?schema=public"

# =================================
# Security
# =================================
# JWT Secret (CHANGE IN PRODUCTION - min 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
JWT_EXPIRATION=7d
JWT_REFRESH_EXPIRATION=30d

# =================================
# CORS
# =================================
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://www.tooldo.app

# =================================
# AWS (quando aplicável)
# =================================
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET_NAME=

# =================================
# Logging
# =================================
LOG_LEVEL=debug
# Options: error, warn, log, debug, verbose

# =================================
# Rate Limiting
# =================================
THROTTLE_TTL=60
THROTTLE_LIMIT=10

# =================================
# Swagger/OpenAPI
# =================================
# Disable in production for security
SWAGGER_ENABLED=true

# =================================
# Email Configuration (SMTP)
# =================================
# Para habilitar envio real de emails, configure SMTP_USER e SMTP_PASSWORD
# Se não configurados, os emails serão apenas logados no console (modo desenvolvimento)

# Email Provider: 'smtp' ou 'aws-ses' (padrão: 'smtp')
EMAIL_PROVIDER=smtp

# SMTP Configuration (obrigatórias para envio real)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=false
SMTP_REQUIRE_TLS=true

# Email Remetente
EMAIL_FROM=
EMAIL_FROM_NAME=Tooldo

# Frontend URL (para links nos emails)
FRONTEND_URL=http://localhost:3001

# =================================
# AI / OpenAI Configuration
# =================================
# Para habilitar geração de planos de ação com IA, configure OPENAI_API_KEY
# Se não configurada, será usado um serviço stub com sugestões mockadas

# OpenAI API Key (obtenha em: https://platform.openai.com/api-keys)
OPENAI_API_KEY=

# =================================
# Twilio (SMS e WhatsApp)
# =================================
# Para habilitar notificações por SMS e WhatsApp, configure as credenciais do Twilio
# Documentação completa: docs/TWILIO_SETUP.md

# Credenciais Twilio (OBRIGATÓRIAS para SMS/WhatsApp)
# Obtenha em: https://console.twilio.com/
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# WhatsApp Content Template (OPCIONAL - apenas se usar WhatsApp)
# Crie templates em: https://console.twilio.com/us1/develop/sms/content-editor
# Após aprovação do WhatsApp, copie o Content SID aqui
TWILIO_WHATSAPP_OVERDUE_ACTION_CONTENT_SID=
